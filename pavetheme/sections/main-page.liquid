<div class="pave-container" style="padding-block:var(--sp-16);">
  <h1 class="pave-display" style="font-size:clamp(28px,4vw,44px);color:var(--heading);margin:0 0 var(--sp-8);">{{ page.title }}</h1>
  <div class="pave-rte">{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
