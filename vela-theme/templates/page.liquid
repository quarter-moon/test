{% layout 'theme' %}

<div class="container section">
  <div style="max-width:680px; margin:0 auto;">
    <h1 style="font-family:var(--font-heading); font-size:clamp(28px,6vw,56px); font-weight:300; margin-bottom:32px; line-height:1.1;">{{ page.title }}</h1>
    <div class="article-body" style="padding:0;">
      {{ page.content }}
    </div>
  </div>
</div>
